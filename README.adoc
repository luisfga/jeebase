## JEEBase (Wildfly Version)

<pending mavem command>

### Uber Jar

----
mvn clean install tomee:exec
<pending maven build command>
<pending java run command>
----

#JPA #Deltaspike #JSF #SecurityAPI #Docker